//In Js, concat() is a method used to join (combine) two or more strings into one.

// Syntax 
// string1.concat(string2, string3, ..., stringN)

let str1 = "Hello";
let str2 = "USS";

let result = str1.concat(" ", str2);
console.log(result);


